#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char x = 'a', y = 'A';

while (x <= 'z')
{
putchar(x);
x++;
}

while (y <= 'Z')
{
putchar(toupper(y));
x++;
}
putchar('\n');
return (0);
}
